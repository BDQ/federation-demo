{
  "name": "federation-demo",
  "scripts": {
    "postinstall": "lerna bootstrap",
    "deploy": "lerna exec --scope=ops -- cdk deploy gql*"
  },
  "devDependencies": {
    "@aws-cdk/aws-lambda": "^1.19.0",
    "concurrently": "latest",
    "nodemon": "latest"
  },
  "dependencies": {
    "@apollo/gateway": "latest",
    "apollo-server": "^2.5.1-alpha.1",
    "graphql": "latest"
  }
}
