{
  "name": "federation-demo",
  "scripts": {
    "postinstall": "lerna bootstrap",
    "deploy": "lerna exec --scope=ops -- npm run deploy"
  },
  "devDependencies": {
    "@aws-cdk/aws-lambda": "^1.19.0",
    "concurrently": "latest",
    "lerna": "^3.20.2",
    "nodemon": "latest"
  },
  "dependencies": {
    "@apollo/gateway": "latest",
    "apollo-server": "^2.5.1-alpha.1",
    "graphql": "latest"
  }
}
